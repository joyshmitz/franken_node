name: Rollback Command Gate

on:
  pull_request:
    paths:
      - "crates/franken-node/src/**"
      - "crates/franken-engine/src/**"
      - "crates/asupersync/src/**"
      - "services/**"
      - "docs/change_summaries/**"
      - "docs/templates/change_summary_template.md"
      - "scripts/check_rollback_command.py"
      - "tests/test_check_rollback_command.py"
  push:
    branches: [main]
    paths:
      - "crates/franken-node/src/**"
      - "crates/franken-engine/src/**"
      - "crates/asupersync/src/**"
      - "services/**"
      - "docs/change_summaries/**"
      - "docs/templates/change_summary_template.md"
      - "scripts/check_rollback_command.py"
      - "tests/test_check_rollback_command.py"

jobs:
  rollback-command:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build changed-file list
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            git fetch --no-tags --depth=1 origin "${{ github.base_ref }}"
            git diff --name-only "origin/${{ github.base_ref }}...HEAD" > /tmp/changed_files.txt
          else
            git diff --name-only HEAD~1...HEAD > /tmp/changed_files.txt
          fi
          cat /tmp/changed_files.txt

      - name: Run rollback-command contract gate
        run: |
          python3 scripts/check_rollback_command.py \
            --changed-files /tmp/changed_files.txt \
            --json > /tmp/rollback_command_gate.json

      - name: Upload gate evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rollback-command-gate
          path: /tmp/rollback_command_gate.json
